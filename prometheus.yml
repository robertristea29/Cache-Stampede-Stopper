global:
  scrape_interval: 5s # How often to check for updates

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'nodejs_api'
    static_configs:
      - targets: ['host.docker.internal:3000'] # Points to your Node.js API